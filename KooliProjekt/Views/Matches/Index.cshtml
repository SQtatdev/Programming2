@model PagedResult<Match>


@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Title</th>

            <th>Title</th>

            <th>Title</th>

            <th>Title</th>

            <th>Title</th>

            <th>Title</th>

            <th>Title</th>

            @(await Component.InvokeAsync<PagerViewComponent>(Model))


            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Results) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tournament.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstTeam.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SecondTeam.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstTeamScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SecondTeamScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.winner)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
