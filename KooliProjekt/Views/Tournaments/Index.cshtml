@model IEnumerable<KooliProjekt.Data.Tournament>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TournmentName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TournamentStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TournamentEnd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TournamentPart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TournamentInfo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TournmentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TournamentStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TournamentEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TournamentPart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TournamentInfo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
